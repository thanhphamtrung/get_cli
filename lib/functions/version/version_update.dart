/// Version check is disabled — dex_cli is not published to pub.dev.
/// Re-enable when the package is published or a custom update channel is set up.
void checkForUpdate() async {
  // no-op: dex_cli is an internal tool, pub.dev version check would always 404
}
